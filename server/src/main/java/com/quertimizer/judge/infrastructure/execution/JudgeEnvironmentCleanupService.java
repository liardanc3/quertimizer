package com.quertimizer.judge.infrastructure.execution;

import org.springframework.stereotype.Service;

@Service
public class JudgeEnvironmentCleanupService {

    // TODO interactive execution도 execution DB orchestrator로 완전히 이동하면 비활성 workspace cleanup을 이 서비스로 통합한다.
}
